# =============================================================
# 前端环境变量配置
# 本仓库仅承载 UI，不再持有任何密钥
# =============================================================

# 后端 API 基地址
#
# 【部署建议】
#
# ✅ 生产环境（通过 Nginx 代理）：留空或不配置
#   这样请求会走相对路径 /api/...，由 Nginx 代理到后端 8888 端口
#   好处：Nginx 的 CORS 配置生效，更安全、性能更好
#
# ⚠️ 开发环境（本地开发）：
#   VITE_API_BASE=http://localhost:8888
#
# ❌ 不建议在生产环境直接填写端口地址（如 http://38.92.8.134:8888）
#   这样会绕过 Nginx，直接访问后端端口，存在安全风险
#
# Netlify 部署（如果使用）：
# VITE_API_BASE=https://your-backend.netlify.app

# VITE_API_BASE=
